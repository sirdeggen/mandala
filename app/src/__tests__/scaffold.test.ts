// Placeholder test — ensures vitest exits 0 during scaffold phase.
// Later tasks will add real tests.
import { describe, it, expect } from 'vitest'

describe('scaffold', () => {
  it('passes', () => {
    expect(true).toBe(true)
  })
})
